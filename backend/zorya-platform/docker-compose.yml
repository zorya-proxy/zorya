version: '3.8'

services:
  zorya-db:
    image: postgres:16-alpine
    container_name: zorya-postgres
    ports:
      - "5432:5432"
    env_file:
      - .env
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
